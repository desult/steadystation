{# MODULAR #}

<div class="feature" style="background-image: url('{{ page.url|e }}/background.jpg')">

    <div>
        <h1>
            {{ page.title }}
        </h1>
    </div>

    <div>
        {{ content|raw }}
    </div>

</div>
