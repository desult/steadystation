{# WORK IN PROGRESS - NO PRODUCTION READY #}

{# MODULAR #}

<div class="feature" style="background-image: url('{{ page.url|e }}/background.jpg')">

    <div>
        <h1>
            {{ page.title }}
        </h1>
    </div>

    <div>
        {{ content|raw }}
    </div>

</div>











{#}
{% set image = page.media.images|first %}
{% if image %}
    {{ image.cropResize(400,400).html('','','align-'~page.header.image_align)|raw }}
{% endif %}
#}
