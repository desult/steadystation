{# WORK IN PROGRESS - NO PRODUCTION READY #}

{# MODULAR #}


    {{ content|raw }}

    <div class="grid-section">

        {% for p in page.collection %}

            {% if p.header.link %}
                <a href="{{ p.url|e }}">
                    <div class="grid-item">
                        <div class="grid-item-img-box">
                            {{ p.media['sample.jpg'].cropResize(500, 500)|raw }}
                        </div>
                    </div>
                </a>
            {% else %}
                <div class="grid-item">
                    <div class="grid-item-img-box">
                        {{ p.media['sample.jpg'].cropResize(500, 500)|raw }}
                    </div>
                </div>
            {% endif %}

        {% endfor %}

    </div>
